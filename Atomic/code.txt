func add(a,b) {
	return a + b;
}
func multiply(a,b) {
	return a * b;
}

# this is a comment

func main() {
	set thing >> add

	set a >> prompt("enter first num: ")
	set b >> prompt("enter second num: ")
	
    write("a + b: ",thing(a,b))

    thing >> multiply

    write("a * b: ",thing(a,b))
}

main()